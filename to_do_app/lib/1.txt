Container(
  height: 200, // specify the height
  width: 200,  // specify the width
  decoration: BoxDecoration(
    image: DecorationImage(
      image: AssetImage('assets/background_image.jpg'), // replace 'assets/background_image.jpg' with your asset path
      fit: BoxFit.cover, // adjust according to your preference
    ),
  ),
  // You can add other widgets inside this Container if needed
)


Container(
                        
                        height: 52,
                        width: 52,
                        decoration: BoxDecoration(
                            color: Colors.white,
                            borderRadius: BorderRadius.circular(26)),
                      )